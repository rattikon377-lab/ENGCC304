#include <stdio.h>

int main() {
    char in[50], process[50], out[50];

    scanf("%s %s %s", in, process, out);

    printf("Input: %s\n", in);
    printf("Process: %s\n", process);
    printf("Output: %s\n", out);

    return 0;
}